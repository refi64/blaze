fun f(x: int):
    return

fun g(n: int) -> int:
    let x = f()
    let y = f(1, 2)
    let z = f(&n, &y)
    let a = f(1)
    let b = f(n)
    return x

fun h:
    let var x = g(1)
    x = &x
    return x

fun i(a: *int): return *a

#[
ERROR
:5:13: error: function expected 1 argument(s), not 0
    let x = f()
            ~
:1:5: note: 'f' declared here
    fun f(x: int):
        ~
:5:13: error: cannot use void value in expression
    let x = f()
            ~
:1:5: note: 'f' declared here
    fun f(x: int):
        ~
:6:13: error: function expected 1 argument(s), not 2
    let y = f(1, 2)
            ~
:1:5: note: 'f' declared here
    fun f(x: int):
        ~
:6:13: error: cannot use void value in expression
    let y = f(1, 2)
            ~
:1:5: note: 'f' declared here
    fun f(x: int):
        ~
:7:13: error: function expected 1 argument(s), not 2
    let z = f(&n, &y)
            ~
:1:5: note: 'f' declared here
    fun f(x: int):
        ~
:7:15: error: function expected argument of type 'int', not '*int'
    let z = f(&n, &y)
              ~
:1:5: note: 'f' declared here
    fun f(x: int):
        ~
:4:7: note: 'n' declared here
    fun g(n: int) -> int:
          ~
:7:13: error: cannot use void value in expression
    let z = f(&n, &y)
            ~
:1:5: note: 'f' declared here
    fun f(x: int):
        ~
:7:9: warning: unused variable 'z'
    let z = f(&n, &y)
        ~
:8:13: error: cannot use void value in expression
    let a = f(1)
            ~
:1:5: note: 'f' declared here
    fun f(x: int):
        ~
:8:9: warning: unused variable 'a'
    let a = f(1)
        ~
:9:13: error: cannot use void value in expression
    let b = f(n)
            ~
:1:5: note: 'f' declared here
    fun f(x: int):
        ~
:9:9: warning: unused variable 'b'
    let b = f(n)
        ~
:14:7: error: types 'int' and '*int' in assignment are not compatible
    x = &x
      ~
:13:13: note: 'x' declared here
    let var x = g(1)
            ~
:13:13: note: 'x' declared here
    let var x = g(1)
            ~
:15:12: error: function should not return a value
    return x
           ~
:12:5: note: function declared here
    fun h:
        ~
:17:24: error: function should not return a value
    fun i(a: *int): return *a
                           ~
:17:5: note: function declared here
    fun i(a: *int): return *a
         ~
#]
